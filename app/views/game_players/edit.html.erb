<h1>Edit</h1>

<%= form_for @player, :url => game_player_path(@game, @player), :method => :patch do |f| %>
  <%= f.label :name, "Player name"%>
  <%= f.text_field :name %>

  <%= f.submit %>

<% end %>